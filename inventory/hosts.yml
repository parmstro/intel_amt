---
# Intel AMT Collection Inventory
# The management_node is where AMT operations execute from

all:
  children:
    management_node:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"

    # Alternative: Remote management node on target network
    # management_node:
    #   hosts:
    #     amt-mgmt-server:
    #       ansible_host: 192.168.254.50
    #       ansible_user: ansible
    #       ansible_become: false
